[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: tests that require a running LM Studio server (skipped by default in CI)
filterwarnings =
    ignore::DeprecationWarning
